# dependencies
/node_modules

# next.js
/.next/
/out/

# production
/build

# env files
# .env* 를 통째로 무시하고 example 만 되돌려 추적한다.
# 전에는 `.env` 와 `.env*.local` 만 막아서 .env.production 이 그대로
# 추적 대상이었다 — 실제 키를 넣으면 그대로 깃에 올라간다.
.env*
!.env*.example

# typescript
*.tsbuildinfo
next-env.d.ts

# misc
.DS_Store
*.pem
npm-debug.log*

# local settings
.claude/settings.local.json
.gstack/

# 에이전트 도구 아티팩트 (프로젝트 코드 아님 — npx skills add 로 재설치 가능)
.agents/
skills-lock.json

# IDE
.idea/

# 로컬 스킬 심볼릭 링크
.claude/skills/
